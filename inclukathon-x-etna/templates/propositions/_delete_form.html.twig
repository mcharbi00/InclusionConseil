<form method="post" action="{{ path('app_propositions_delete', {'id': proposition.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ proposition.id) }}">
    <button class="btn">Delete</button>
</form>
