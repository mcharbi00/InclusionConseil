<form method="post" action="{{ path('app_themes_incluscore_delete', {'id': themes_incluscore.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ themes_incluscore.id) }}">
    <button class="btn">Delete</button>
</form>
