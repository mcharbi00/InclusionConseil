{% extends 'base.html.twig' %}

{% block title %}ThemesIncluscore{% endblock %}

{% block body %}
    <h1>ThemesIncluscore</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ themes_incluscore.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ themes_incluscore.name }}</td>
            </tr>
            <tr>
                <th>Enabled</th>
                <td>{{ themes_incluscore.enabled ? 'Yes' : 'No' }}</td>
            </tr>
            <tr>
                <th>ImgPath</th>
                <td>{{ themes_incluscore.imgPath }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_themes_incluscore_index') }}">back to list</a>

    <a href="{{ path('app_themes_incluscore_edit', {'id': themes_incluscore.id}) }}">edit</a>

    {{ include('themes_incluscore/_delete_form.html.twig') }}
{% endblock %}
